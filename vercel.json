{
    "version":2,
    "builds":[
        {
            "src": "./src/**/*.{js,ts,jsx,tsx}",
            "use":"@vercel/node"
        }
    ],
    "routes":[
        {
            "src": "/.*",
            "dest":"/"
        }
    ]
}